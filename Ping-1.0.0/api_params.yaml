environments:
  - name: dev
    endpoints:
      production:
      sandbox:
  - name: prod
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
